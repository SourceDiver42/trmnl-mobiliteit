---
# auto-reload when files change (`watch: false` to disable)
watch:
  - .trmnlp.yml
  - src

# values of custom fields (defined in src/settings.yml)
custom_fields: 
  accessId: "{{ env.API_ACCESS_ID }}"
  stopId: "{{ env.API_STOP_ID }}"


# override variables
variables:
  trmnl: {}
